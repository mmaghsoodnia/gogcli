# Codex Instructions

Read `mhivereadme.md` first, then `TODO.md`.

Keep secrets in 1Password and never print credential values. Use `gog` for
Google Workspace work and follow the repo's stdout/stderr automation contract.
